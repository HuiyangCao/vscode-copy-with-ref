# 每条命令只包含 name 和 command；多行命令使用 |。
- name: NVIDIA SMI
  command: nvidia-smi

- name: 本地到服务器
  command: |
    rsync -avz --delete \
      --exclude='.venv/' \
      --exclude='logs/' \
      ./ ssh_server:/home/user/project/
